# Copy to `.env` and adjust. All client env vars must be prefixed with VITE_.

# Base URL of the SPK Mainan backend (Spring Boot). Note the `/v1` context path.
VITE_API_BASE_URL=http://localhost:8080/v1
